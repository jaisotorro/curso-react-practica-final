// import { createSelector } from "reselect";

export const getUser = (state) => state.connection.user;

export const getTime = (state) => state.connection.time;

// export const getData = (state) => state.connection.¿?;

